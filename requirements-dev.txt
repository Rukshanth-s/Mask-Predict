# Development/test dependencies.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/python -m pytest tests/
#
# torch installs the CPU build by default from PyPI; the tests need no GPU.
torch
# No numpy cap. This snapshot's only use of a removed alias was np.float in
# fairseq/data/indexed_dataset.py, now spelled np.float64, so numpy 1.x and 2.x
# both work. Do not re-add "numpy<1.24": modern torch wheels are built against
# numpy 2, and pinning down forces a large, pointless download.
numpy
pytest
tqdm
