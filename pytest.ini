[pytest]
testpaths = tests
filterwarnings =
    # fairseq 0.7.2 uses the np.float alias that newer numpy deprecated; this is
    # pre-existing and unrelated to anything under test.
    ignore:.*`np\.float`.*:DeprecationWarning
